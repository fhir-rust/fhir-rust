# AI Statement — see the repository root

The AI statement for this repository is [`/AI_STATEMENT.md`](../../AI_STATEMENT.md),
at the root, where every other special file in this directory's checklist
lives and where GitHub, tooling, and readers look for it.

A full draft used to live here as well, written separately from the root
document and divergent from it — the exact failure mode a single source
exists to prevent, and one this directory's own `index.md` flagged the day
both were written. Resolved 2026-08-26: the root document is the source. It
carries everything this draft had, plus the failure-mode register (§8) the
draft lacked; the draft's one section with no root counterpart, "Rules for
contributors", lives in [`CONTRIBUTING.md`](../../CONTRIBUTING.md) §"Using
AI tools", stated near-verbatim. The draft's text remains in this file's
git history.
