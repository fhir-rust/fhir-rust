# Fail rather than warn on package.json's engines mismatch. Without this,
# npm install/ci quietly proceeds on a too-old Node — which is exactly how
# the site's package-lock.json ended up out of sync with package.json in an
# earlier session (Node 19.2.0 locally vs. this repo's real floor).
engine-strict=true
